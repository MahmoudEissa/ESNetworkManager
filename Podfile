# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ESNetworkManager' do
    pod 'Alamofire', '~> 5.0'
    pod 'ObjectMapper', '~> 3.5'
    pod "PromiseKit", "~> 6.8"
    pod 'RxSwift', '~> 5'
    target 'ESNetworkManagerTests' do
           inherit! :search_paths
    end
end

